server.port=8080
server.from=QA
api.authUrl=${API_AUTH_URL}
springdoc.server-url=${SPRINGDOC_SERVER_URL:}
springdoc.api-docs.enabled=false
springdoc.swagger-ui.enabled=false
app.security.swagger-public=false

spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}
security.jwt.auto-generate-keys=false
security.jwt.private-key=${SECURITY_JWT_PRIVATE_KEY}
security.jwt.public-key=${SECURITY_JWT_PUBLIC_KEY}
security.jwt.key-id=${SECURITY_JWT_KEY_ID}
security.jwt.refresh-token-encryption-key=${QA_SECURITY_JWT_REFRESH_TOKEN_ENCRYPTION_KEY:${SECURITY_JWT_REFRESH_TOKEN_ENCRYPTION_KEY:}}
security.jwt.refresh-token-allow-body=false
security.jwt.refresh-token-return-in-body=false
app.security.permissions.deny-when-unknown=true
app.security.permissions.client-allow-paths=${APP_SECURITY_CLIENT_ALLOW_PATHS:/worklist/worklist-received-study}
app.security.permissions.client-allow-client-ids=${APP_SECURITY_CLIENT_ALLOW_CLIENT_IDS:}

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${SPRING_MAIL_USERNAME:}
spring.mail.password=${SPRING_MAIL_PASSWORD:}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

telegram.chat.id=${TELEGRAM_CHAT_ID:}
telegram.api.token=${TELEGRAM_API_TOKEN:}

cors.allowedOrigins=${CORS_ALLOWED_ORIGINS:}
cors.allowCredentials=${CORS_ALLOW_CREDENTIALS:false}
app.security.cookies.secure=true
pacs.result.static-auth.enabled=${PACS_RESULT_STATIC_AUTH_ENABLED:true}
pacs.result.static-auth.api-key=${PACS_RESULT_API_KEY:}
pacs.result.upload-root=${PACS_RESULT_UPLOAD_ROOT:${HOSPITAL_IMAGE_ROOT_PATH:/var/ut-image}}
pacs.result.max-image-bytes=${PACS_RESULT_MAX_IMAGE_BYTES:10485760}

logging.level.com.ut.emr=INFO
