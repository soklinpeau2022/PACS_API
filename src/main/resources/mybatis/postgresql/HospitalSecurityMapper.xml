<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ut.emrPacs.mapper.auth.HospitalSecurityMapper">

    <select id="countActiveHospitalById" resultType="java.lang.Long">
        SELECT COUNT(1)
        FROM hospitals h
        WHERE h.id = #{hospitalId}
          AND h.is_active = 1
    </select>

    <select id="countActiveUserHospital" resultType="java.lang.Long">
        SELECT COUNT(1)
        FROM user_hospitals uh
        JOIN hospitals h ON h.id = uh.hospital_id
        WHERE uh.user_id = #{userId}
          AND uh.hospital_id = #{hospitalId}
          AND uh.is_active = 1
          AND h.is_active = 1
    </select>

</mapper>

